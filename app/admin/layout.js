import "./admin-console.css";

export const metadata = {
  title: "Tonaura Admin Console",
  description: "Internal admin for Tonaura accounts, waitlist, contact, and Premium.",
};

export default function AdminLayout({ children }) {
  return children;
}
